<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 콘솔 출력용 (로컬 개발) -->
    <springProfile name="dev,local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <!-- Docker 환경용 JSON 로그 -->
    <springProfile name="docker,prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSSSSSSSXXX</pattern>
                    </timestamp>
                    <version/>
                    <logLevel/>
                    <message/>
                    <mdc/>
                    <loggerName/>
                    <thread/>
                    <stackTrace/>
                    <pattern>
                        <pattern>
                        {
                            "springAppName": "${SPRING_APPLICATION_NAME:-apimanagement}",
                            "service": "${SERVICE_NAME:-apimanagement}",
                            "pid": "${PID:-unknown}"
                        }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
        
        <!-- Logstash TCP appender -->
        <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
            <destination>${LOGSTASH_HOST:-systemmanagementsvc-logstash-1}:${LOGSTASH_PORT:-5044}</destination>
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSSSSSSSXXX</pattern>
                    </timestamp>
                    <version/>
                    <logLevel/>
                    <message/>
                    <mdc/>
                    <loggerName/>
                    <thread/>
                    <stackTrace/>
                    <pattern>
                        <pattern>
                        {
                            "springAppName": "${SPRING_APPLICATION_NAME:-apimanagement}",
                            "service": "${SERVICE_NAME:-apimanagement}",
                            "pid": "${PID:-unknown}",
                            "environment": "${SPRING_PROFILES_ACTIVE:-unknown}"
                        }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
            <!-- 연결 실패 시에도 애플리케이션이 계속 실행되도록 설정 -->
            <includeCallerData>true</includeCallerData>
            <keepAliveDuration>5 minutes</keepAliveDuration>
            <connectionTimeout>10 seconds</connectionTimeout>
            <reconnectionDelay>10 seconds</reconnectionDelay>
        </appender>
        
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="LOGSTASH" />
        </root>
    </springProfile>
</configuration>